import { Entity } from 'marker-engine';
export class NPC extends Entity{
    constructor(context={}){
        
    }
    
    communicate(world, activePlayerQuests){
        
    }
    
    logNpcEvent(event){
        
    }
    
}